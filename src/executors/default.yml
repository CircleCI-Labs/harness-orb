description: >
  Default executor for running a Harness/Drone plugin container. Must be `machine`, not
  `docker` + setup_remote_docker: plugin containers need a real bind-mount of the checkout
  (setup_remote_docker's Docker host is a separate machine and cannot bind-mount the job's
  filesystem - see CircleCI's own Docker Compose volume-mounting docs), and some plugins
  require --privileged, which the docker executor and self-hosted Container Runner both disallow.

parameters:
  image:
    type: string
    default: ubuntu-2404:current
    description: The machine image to run the plugin container on.
  docker_layer_caching:
    type: boolean
    default: false
    description: >
      Enable CircleCI's Docker Layer Caching. Defaults to false, measured rather than assumed:
      against plugins/docker (~208MB), two independent real-CircleCI runs showed no measurable
      pull-time improvement from DLC, while DLC itself added ~3s of spin-up/teardown overhead on
      top of its billed, plan-gated cost. See docs/ROADMAP.md's "Image caching economics" section
      for the measured numbers and job references, and docs/LIMITS.md's "Caching the plugin
      image" section for user-facing guidance. Available as an opt-in if your own plugin image or
      network conditions differ from what was measured.
  resource_class:
    type: string
    default: medium
    description: Resource class for the executor.

machine:
  image: << parameters.image >>
  docker_layer_caching: << parameters.docker_layer_caching >>
resource_class: << parameters.resource_class >>
